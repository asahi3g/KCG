namespace Enums
{

    // this enum is used to attach different models
    // to the agent when he is using items
    public enum ItemAnimationSet
    {
        Default,
        HoldingPistol, // used to play pistol animations instead of default
        HoldingRifle, // used to play rifle animations instead of default
        HoldingSword,
    }
}
